# Copy to .env.local (gitignored, auto-loaded by `bun run pool`).
# Least-privilege reader — never admin, never NEXT_PUBLIC_* (those ship to the browser).
CLICKHOUSE_URL=https://ijyh61ehht.us-central1.gcp.clickhouse.cloud:8443
CLICKHOUSE_USER=pragma_reader
CLICKHOUSE_PASSWORD=fill-me-in
